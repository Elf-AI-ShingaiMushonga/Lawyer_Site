# UFC Embedded Deployment Note

The UFC predictor is deployed as part of the main intranet service and no longer has a standalone EC2 bootstrap path.

Use the root deployment guide in `README.md` (`deploy/ubuntu/*` assets) and access UFC at `/ufc/`.
